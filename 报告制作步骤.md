# TeamFlow 课设报告制作步骤

## 1. 这份文档的用途

本文件用于指导别的 AI（优先使用 Codex）基于当前项目仓库内容和 `要求.md`，帮助完成《软件工程》课程设计报告。  
重点不是让 AI 乱写，而是：

1. 必须严格结合当前项目 `TeamFlow` 的真实功能、页面、数据库和代码结构来写。
2. 必须严格覆盖老师在 `要求.md` 中提出的文档内容与建模图要求。
3. 所有建模图、数据库图、用例图、类图、界面图，都要先由 AI 生成“可核对的文字版结构说明”，再让 Codex 调用 `gpt-image-2` 生成参考图。
4. 生成的图主要作为“我后续手动重绘的参考底稿”，因此必须规范、简洁、清晰，符合软件工程绘图习惯，不能追求花哨。

---

## 2. 本项目的基本定位

本项目属于老师给出的第二个方向：

- `小组协作过程管理软件`

当前项目名称可写为：

- 中文题目：`基于 AI 驱动协作过程审计的小组协作过程管理软件的分析与设计`
- 英文名可选：`TeamFlow`

项目定位应统一表述为：

- 面向大学课程设计/小组项目场景的协作过程管理平台；
- 核心解决任务分配、进度跟踪、团队沟通、文档协作、贡献评估、风险预警、Git 协作可视化、AI 任务规划与报告生成等问题；
- 强调“过程可追溯、证据可查看、贡献可量化、风险可预警”。

---

## 3. AI 写报告前必须先读取的材料

让 Codex 先阅读并提炼以下文件，不允许脱离项目瞎编：

1. `要求.md`
2. `README.md`
3. `doc/开发文档.md`
4. `backend/app/main.py`
5. `backend/app/core/config.py`
6. `backend/app/models/project.py`
7. `backend/app/models/task.py`
8. `backend/app/models/document.py`
9. `backend/app/models/message.py`
10. `backend/app/models/collaboration.py`
11. `backend/app/models/user.py`
12. `backend/app/api/auth.py`
13. `backend/app/api/projects.py`
14. `backend/app/api/tasks.py`
15. `backend/app/api/messages.py`
16. `backend/app/api/documents.py`
17. `backend/app/api/collaboration.py`
18. `backend/app/api/ai_service.py`
19. `frontend/package.json`
20. `frontend/src/router/index.ts`
21. `frontend/src/views/project/` 下各页面文件

如果 AI 没有先读这些文件，就不要让它开始写正文和画图。

---

## 4. 当前项目可确认的真实内容

以下内容可以作为 AI 写报告和出图时的“事实基础”：

### 4.1 技术栈

- 前端：`Vue 3 + TypeScript + Vite + Vue Router + Pinia + Element Plus + ECharts + G6`
- 后端：`FastAPI + SQLAlchemy + Pydantic`
- 数据库：当前配置默认是 `SQLite`
- 鉴权：`JWT`
- 通信：包含 `WebSocket`
- AI 能力：项目内已有 AI 任务规划、周报/报告生成相关接口

### 4.2 已有主要功能模块

- 用户注册、登录、个人信息管理
- 项目创建、成员邀请、角色管理
- 项目仪表盘
- 任务管理、状态更新、进度跟踪、里程碑
- 项目消息/讨论
- 协作文档与版本
- 文件资源管理
- 协作图谱（Graph）
- 贡献度统计
- 风险预警
- Git 仓库/提交相关数据管理
- AI 任务规划与 AI 报告生成

### 4.3 前端已有主要页面

根据路由可确认已有页面：

- 登录页
- 注册页
- 项目列表页
- 项目仪表盘页
- 任务管理页
- 消息讨论页
- 文档列表页
- 文档编辑页
- 文件管理页
- 协作图谱页
- 贡献分析页
- 风险预警页
- Git 协作页
- AI 助手页
- 项目设置页

### 4.4 后端已有核心数据对象

已能从模型中确认的核心实体包括：

- 用户 `User`
- 项目 `Project`
- 项目成员 `ProjectMember`
- 任务 `Task`
- 任务依赖 `TaskDependency`
- 里程碑 `Milestone`
- 文档 `Document`
- 文档版本 `DocumentVersion`
- 消息 `Message`
- 协作事件 `CollaborationEvent`
- 贡献得分 `ContributionScore`
- 贡献证据 `ContributionEvidence`
- 风险预警 `RiskAlert`
- 互评 `PeerEvaluation`
- 文件资源 `FileResource`
- Git 仓库 `GitRepository`
- Git 提交 `GitCommit`
- AI 任务建议 `AITaskSuggestion`
- AI 报告历史 `AIReportHistory`

### 4.5 报告中必须实事求是的地方

1. 能从代码和页面确认的，按“已实现”写。
2. 在 `doc/开发文档.md` 中提出、但代码实现不完整的，可写成“设计目标/扩展功能/规划功能”。
3. 不允许把未实现功能全部写成“已完全实现”。
4. 教师视角、评分参考、过程审计、贡献证据链等内容，可以作为本系统亮点重点写，但要注意区分“已有数据结构支持”和“完整业务闭环是否全部实现”。

---

## 5. `要求.md` 中必须满足的硬性要求

AI 写报告时必须逐条满足以下要求：

### 5.1 报告内容必须包含

1. 可行性分析与需求描述
2. 传统方法学分析
3. 面向对象方法学分析与设计
4. 数据库设计
5. 界面设计
6. 结论与展望（创新点）

### 5.2 传统方法学必须至少包含

1. 数据流图
2. 至少两张：`环境图` 和 `1 层数据流图`
3. 系统结构图
4. 过程设计可选
5. 数据库设计

### 5.3 面向对象方法学必须至少包含

1. 用例图
2. 类图
3. 顺序图可选

### 5.4 界面设计要求

1. 页面不能太少
2. 最好准备 4 张以上页面设计图
3. 页面布局要统一，符合系统主题

### 5.5 绘图评分重点

AI 出图必须围绕这几条做：

1. 符号正确
2. 连线整洁
3. 功能完整
4. 文字说明合理
5. 图与要求一致

---

## 6. 推荐的报告结构

让 AI 按下面结构输出，最稳妥：

1. 绪论
2. 项目背景与选题意义
3. 可行性分析
4. 需求分析
5. 传统方法学分析与设计
6. 面向对象分析与设计
7. 数据库设计
8. 界面设计
9. 系统实现概述
10. 结论与展望
11. 创新点总结

说明：

- “传统方法学分析”和“面向对象分析”要分开写，不能混在一起。
- “系统实现概述”可以简要结合现有前后端项目结构写。
- 如果老师最后只看建模图和界面图，这份结构也不会丢分。

---

## 7. 图件总清单

本项目建议至少输出以下图件：

### 7.1 必画图

1. 系统环境图（传统方法学）
2. 1 层数据流图（传统方法学）
3. 系统结构图（传统方法学）
4. 用例图（面向对象）
5. 类图（面向对象）
6. E-R 图或数据库关系图
7. 4 张以上界面设计图

### 7.2 选画图

1. 任务状态流转图
2. 文档协作顺序图
3. AI 任务规划顺序图
4. 协作事件流转图

如果时间够，建议多补 1 张顺序图，这样更完整。

---

## 8. 所有图统一遵守的绘图规范

这一节很重要，必须原样要求 AI 遵守。

### 8.1 通用规范

1. 统一使用白底。
2. 优先使用黑色、深灰、蓝黑色线条。
3. 不要使用花里胡哨的 3D 风格、手绘风、插画风。
4. 不要使用夸张阴影、玻璃拟态、海报式排版。
5. 所有元素对齐，间距均匀，避免文字挤压。
6. 中文标签必须简洁、规范、专业。
7. 箭头方向必须明确。
8. 同类元素样式必须统一。
9. 图中英文缩写尽量少，若使用必须在正文解释。
10. 图要适合后续人工临摹，结构要清楚，不能太密。

### 8.2 传统方法学图规范

1. 外部实体、处理过程、数据存储、数据流必须用规范符号表达。
2. 环境图要突出“系统”和“外部实体”的信息交换。
3. 1 层数据流图要体现主要业务处理过程及核心数据存储。
4. 数据流名称必须是“信息/数据内容”，不能写成模块名。
5. 连线尽量横平竖直。

### 8.3 UML 图规范

1. 用例图必须有参与者、系统边界、用例、关联关系。
2. 类图必须写清类名、主要属性、主要方法、类之间关系。
3. 继承、关联、依赖、聚合、组合等关系符号不能混用。
4. 类图不要求把所有字段都塞进去，只放最关键、最能体现结构的内容。

### 8.4 数据库图规范

1. 表名统一。
2. 主键、外键尽量标注清楚。
3. 关系方向清晰。
4. 表过多时可只展示核心业务表，不必把每个辅助表都挤进去。

### 8.5 界面图规范

1. 页面结构要真实对应当前项目页面。
2. 页面命名要统一。
3. 组件布局要符合管理系统常见结构。
4. 颜色克制，以课程设计报告风格为主。
5. 不要做成营销官网风格。

---

## 9. 先文字、后出图的强制流程

让 Codex 工作时必须遵循下面顺序，不能一上来就直接画图：

### Step 1：先总结项目事实

输出内容包括：

1. 项目名称
2. 项目背景
3. 核心用户
4. 主要功能模块
5. 技术栈
6. 已实现功能
7. 规划/扩展功能

### Step 2：先列报告大纲

要求按 `要求.md` 补齐必写章节。

### Step 3：先列“图件规划表”

每张图先输出以下内容：

1. 图名
2. 所属章节
3. 该图作用
4. 图中必须出现的元素
5. 图中不能出现的错误
6. 建议布局

### Step 4：先输出每张图的“文字版结构草稿”

例如一张图至少要先给出：

1. 元素清单
2. 元素层级
3. 连接关系
4. 箭头方向
5. 标签文字
6. 左中右/上中下布局建议

### Step 5：确认无误后，再调用 `gpt-image-2` 出图

要求：

1. 每次只画一张图
2. 每张图必须先有独立提示词
3. 每张图生成后要做一次文字校对
4. 若文字、箭头或结构错误，重新生成

### Step 6：最后再回填正文

把图的说明、图意分析、与本系统的关系写进报告正文。

---

## 10. 每张图都要先输出的模板

让 AI 对每张图都先按以下模板输出：

```md
图名：
所属章节：
图类型：
绘图目的：

一、应出现的元素
1.
2.
3.

二、元素之间的关系
1.
2.
3.

三、版式建议
1.
2.

四、图中标注文字
1.
2.

五、容易出错的地方
1.
2.

六、用于 gpt-image-2 的绘图提示词
...
```

---

## 11. 本项目各图建议怎么画

下面这一节是给 AI 的具体业务约束，尽量按这里来。

### 11.1 系统环境图

建议图名：

- `小组协作过程管理软件系统环境图`

建议核心外部实体：

1. 组长
2. 普通成员
3. 指导教师
4. Git 仓库/代码平台
5. AI 服务

中心处理对象：

- `TeamFlow 小组协作过程管理软件`

建议数据流：

1. 项目创建/成员管理请求
2. 任务分配与进度更新信息
3. 消息讨论与文档编辑内容
4. 项目进度查看/贡献查看/风险查看
5. Git 提交记录/仓库同步信息
6. AI 任务规划请求/AI 报告生成结果

要求：

- 环境图只体现系统与外部对象之间的信息交换，不要画得像类图或结构图。

### 11.2 1 层数据流图

建议主要处理过程：

1. 用户与项目管理
2. 任务协同管理
3. 消息与沟通管理
4. 文档与文件管理
5. 协作事件与贡献分析
6. 风险预警与 AI 辅助

建议主要数据存储：

1. 用户库
2. 项目库
3. 任务库
4. 消息库
5. 文档库
6. 文件库
7. 协作事件库
8. 贡献评分库
9. 风险预警库
10. Git 数据库
11. AI 结果库

建议外部实体：

1. 组长
2. 普通成员
3. 指导教师
4. Git 仓库
5. AI 服务

要求：

- 处理过程编号可用 `1.0`、`2.0`、`3.0` 这种格式。
- 数据流名称要写成“用户信息”“任务数据”“贡献结果”“预警结果”这种名词短语。

### 11.3 系统结构图

建议采用功能分解方式：

顶层：

- TeamFlow 小组协作过程管理软件

第二层模块：

1. 用户与权限模块
2. 项目空间模块
3. 任务管理模块
4. 消息沟通模块
5. 文档协作模块
6. 文件管理模块
7. 协作图谱模块
8. 贡献分析模块
9. 风险预警模块
10. Git 协作模块
11. AI 智能辅助模块

第三层可以再拆：

- 用户与权限模块：注册、登录、个人信息
- 项目空间模块：项目创建、成员邀请、角色设置、项目设置
- 任务管理模块：任务创建、分配、状态更新、进度跟踪、里程碑
- 文档协作模块：文档创建、版本管理、内容编辑
- AI 智能辅助模块：AI 任务规划、AI 周报/报告生成

要求：

- 这张图体现模块从属关系，不要画成数据库关系图。

### 11.4 用例图

建议参与者：

1. 组长
2. 普通成员
3. 指导教师
4. AI 服务（可作为外部支持系统，若画不稳可以不放）

建议核心用例：

1. 注册登录
2. 创建项目
3. 邀请成员
4. 管理成员角色
5. 创建任务
6. 分配任务
7. 更新任务进度
8. 发送项目消息
9. 编辑协作文档
10. 上传项目文件
11. 查看协作图谱
12. 查看贡献分析
13. 查看风险预警
14. 绑定/查看 Git 协作数据
15. 使用 AI 生成任务规划
16. 使用 AI 生成周报/报告
17. 查看项目过程与评分依据（教师）

要求：

- 组长和普通成员的能力要有区分。
- 教师更多体现“查看、评价、参考”的用例。

### 11.5 类图

建议优先画核心业务类，不要把所有类都画上去。

建议保留的核心类：

1. User
2. Project
3. ProjectMember
4. Task
5. TaskDependency
6. Milestone
7. Document
8. DocumentVersion
9. Message
10. CollaborationEvent
11. ContributionScore
12. ContributionEvidence
13. RiskAlert
14. FileResource
15. GitRepository
16. GitCommit
17. AITaskSuggestion
18. AIReportHistory

建议关系重点：

1. User 与 Project 通过 ProjectMember 建立多对多关系
2. Project 与 Task 一对多
3. Task 与 TaskDependency 相关联
4. Project 与 Document 一对多
5. Document 与 DocumentVersion 一对多
6. Project 与 Message 一对多
7. Project 与 CollaborationEvent 一对多
8. Project 与 RiskAlert 一对多
9. Project 与 GitRepository 一对多
10. GitRepository 与 GitCommit 一对多
11. Project 与 AITaskSuggestion / AIReportHistory 一对多

类图建议字段只选关键字段：

- `id`
- `name/title`
- `description/content`
- `status`
- `created_at`
- 关键外键

方法可选写法：

- `createProject()`
- `inviteMember()`
- `createTask()`
- `updateTaskStatus()`
- `saveDocumentVersion()`
- `recordEvent()`
- `recalculateContribution()`
- `generateAIReport()`

### 11.6 数据库 E-R 图/关系图

建议分两种画法二选一：

1. 画标准 E-R 图
2. 画数据库关系图

如果想稳一点，推荐画数据库关系图。

建议重点展示这些表：

1. user
2. project
3. project_member
4. task
5. task_dependency
6. milestone
7. document
8. document_version
9. message
10. collaboration_event
11. contribution_score
12. contribution_evidence
13. risk_alert
14. file_resource
15. git_repository
16. git_commit
17. ai_task_suggestion
18. ai_report_history

要求：

- 核心关系清晰即可，不要求把每个字段全部画满。
- 主键、外键建议标注。

### 11.7 界面设计图

建议优先出这 6 张：

1. 登录页
2. 项目列表页
3. 项目仪表盘页
4. 任务管理页
5. 文档协作页
6. 协作图谱/贡献分析页

如果还想补：

1. 风险预警页
2. AI 助手页

界面图必须结合现有项目页面结构，不允许凭空换成电商、社交或博客风格。

---

## 12. 给 Codex 的工作步骤

下面这部分可以直接作为“执行顺序”。

### 第一步：让 Codex 先分析项目

提示词：

```text
请先阅读当前仓库中的要求.md、doc/开发文档.md、前后端核心代码与路由，不要立刻写报告。
请输出：
1. 项目名称与定位
2. 老师要求中的必写内容
3. 当前项目已实现功能
4. 当前项目可作为设计扩展但不应写成完全实现的功能
5. 报告建议目录
6. 本项目应该绘制的全部图件清单
要求：必须基于项目实际代码，不要虚构功能。
```

### 第二步：让 Codex 生成报告大纲

提示词：

```text
请基于当前项目 TeamFlow 和 要求.md，生成一份软件工程课设报告详细大纲。
要求：
1. 必须包含可行性分析、需求描述、传统方法学分析、面向对象分析、数据库设计、界面设计、结论与展望。
2. 传统方法学和面向对象方法学必须分开写。
3. 标明每一章需要插入哪些图。
4. 标明哪些内容应写“已实现”，哪些应写“设计/扩展”。
```

### 第三步：让 Codex 先输出全部图的文字结构说明

提示词：

```text
请不要直接出图。
请先为本项目需要的每一张图输出详细的文字版结构草稿。
每张图都要包含：
1. 图名
2. 图类型
3. 绘图目的
4. 应出现的元素
5. 元素之间关系
6. 箭头方向
7. 建议布局
8. 图中标注文字
9. 容易出错的地方
要求：必须符合软件工程课程设计建模规范，并且结合 TeamFlow 的真实功能模块。
```

### 第四步：让 Codex 逐张使用 `gpt-image-2` 出图

提示词：

```text
现在请逐张为本项目生成建模图参考图。
要求：
1. 使用 gpt-image-2 生成，不要一次生成多张。
2. 每次只处理一张图。
3. 先复述该图的结构，再生成图片。
4. 图必须符合软件工程绘图规范，白底、清晰、整洁、可供人工照着重绘。
5. 不要使用海报风、插画风、拟物风。
6. 如果图片中的文字过于混乱，请改为更简洁的标签并重新生成。
```

### 第五步：让 Codex 根据图回填正文

提示词：

```text
请根据已经确定的项目事实、报告大纲和图件内容，开始撰写软件工程课设报告正文。
要求：
1. 必须严格围绕 TeamFlow 项目当前代码与文档内容。
2. 对于已实现的功能写成系统现状；对于尚未完整实现但已有设计依据的功能写成设计扩展。
3. 每一张图前后都要有对应的文字说明，包括图的作用、主要内容、与系统的关系。
4. 语言风格要符合大学软件工程课程设计报告，不要写成产品营销文案。
```

### 第六步：让 Codex 做最终检查

提示词：

```text
请对整份课设报告做一次最终审查，重点检查：
1. 是否完整覆盖要求.md中的全部必写项
2. 是否区分了传统方法学和面向对象方法学
3. 是否至少包含环境图、1层数据流图、系统结构图、用例图、类图、数据库图、4张以上界面图
4. 图名、章节名、术语是否统一
5. 是否存在把未实现功能写成完全实现的情况
6. 是否存在与代码不符的描述
请输出修正建议和最终可交稿版本。
```

---

## 13. 给 `gpt-image-2` 的统一出图规则

这一段可以反复复用。

```text
你现在需要为《软件工程》课程设计报告生成一张规范的软件工程图示参考图，后续我会人工重绘。

统一要求：
1. 白色背景，扁平化、规范化、干净整洁。
2. 采用软件工程课程设计常见图示风格，不要插画风，不要海报风。
3. 结构清晰，元素均匀对齐，连线明确。
4. 文字简洁规范，适合人工照着重画。
5. 优先黑灰蓝配色，避免花哨渐变。
6. 不要添加无关装饰元素。
7. 图像比例适合放入课程设计报告。
8. 如果是 UML 图、数据流图、E-R 图，必须尽量符合对应图种的标准画法。
```

---

## 14. 各类图可直接复用的出图提示词模板

### 14.1 环境图提示词模板

```text
请生成一张《小组协作过程管理软件系统环境图》参考图，用于软件工程课程设计报告。
中心对象为“TeamFlow 小组协作过程管理软件”。
外部实体包括：组长、普通成员、指导教师、Git 仓库/代码平台、AI 服务。
请用规范的数据流图环境图风格表现系统与外部实体之间的信息交换，展示项目管理、任务分配、进度更新、消息沟通、文档协作、Git 数据同步、AI 任务规划与 AI 报告生成等数据流。
要求白底、专业、简洁、对齐规整、箭头清晰、文字简洁，适合人工临摹重绘。
```

### 14.2 1 层数据流图提示词模板

```text
请生成一张《小组协作过程管理软件 1 层数据流图》参考图，用于软件工程课程设计报告。
外部实体包括：组长、普通成员、指导教师、Git 仓库、AI 服务。
主要处理过程包括：1.0 用户与项目管理、2.0 任务协同管理、3.0 消息与沟通管理、4.0 文档与文件管理、5.0 协作事件与贡献分析、6.0 风险预警与 AI 辅助。
主要数据存储包括：用户库、项目库、任务库、消息库、文档库、文件库、协作事件库、贡献评分库、风险预警库、Git 数据库、AI 结果库。
请按规范的数据流图方式绘制，突出数据流和处理过程，不要画成架构图。
要求白底、整洁、箭头明确、标签简洁、布局均衡，适合人工照着重画。
```

### 14.3 系统结构图提示词模板

```text
请生成一张《TeamFlow 系统结构图》参考图，用于软件工程课程设计报告。
顶层是 TeamFlow 小组协作过程管理软件。
第二层模块包括：用户与权限模块、项目空间模块、任务管理模块、消息沟通模块、文档协作模块、文件管理模块、协作图谱模块、贡献分析模块、风险预警模块、Git 协作模块、AI 智能辅助模块。
每个二级模块下可以有 2 到 4 个三级子功能，例如注册登录、成员邀请、任务分配、进度跟踪、文档版本、贡献统计、风险扫描、AI 任务规划、AI 报告生成等。
请用典型软件结构图/功能分解图风格绘制，层级清楚，对齐规范，白底简洁，适合人工重绘。
```

### 14.4 用例图提示词模板

```text
请生成一张《TeamFlow 用例图》参考图，用于软件工程课程设计报告。
参与者包括：组长、普通成员、指导教师。
系统边界为：TeamFlow 小组协作过程管理软件。
核心用例包括：注册登录、创建项目、邀请成员、管理成员角色、创建任务、分配任务、更新任务进度、发送项目消息、编辑协作文档、上传项目文件、查看协作图谱、查看贡献分析、查看风险预警、查看 Git 协作数据、使用 AI 生成任务规划、使用 AI 生成周报/报告、教师查看项目过程与评分依据。
请使用规范 UML 用例图风格绘制，布局清晰，文字简洁，关联准确，白底，适合人工临摹。
```

### 14.5 类图提示词模板

```text
请生成一张《TeamFlow 核心类图》参考图，用于软件工程课程设计报告。
请仅绘制核心业务类：User、Project、ProjectMember、Task、TaskDependency、Milestone、Document、DocumentVersion、Message、CollaborationEvent、ContributionScore、ContributionEvidence、RiskAlert、FileResource、GitRepository、GitCommit、AITaskSuggestion、AIReportHistory。
请体现关键属性、关键方法和主要关系，包括一对多、多对多、中间关联类等。
要求遵循 UML 类图规范，结构清晰，不要把所有字段塞满，重点体现系统核心业务结构，白底、整洁、适合人工重绘。
```

### 14.6 数据库关系图提示词模板

```text
请生成一张《TeamFlow 数据库关系图》参考图，用于软件工程课程设计报告。
主要数据表包括：user、project、project_member、task、task_dependency、milestone、document、document_version、message、collaboration_event、contribution_score、contribution_evidence、risk_alert、file_resource、git_repository、git_commit、ai_task_suggestion、ai_report_history。
请标注主键和主要外键关系，突出项目、用户、任务、文档、协作事件、贡献分析、风险预警、Git、AI 相关数据之间的联系。
要求采用规范数据库关系图风格，白底、清晰、整洁、可供人工重绘。
```

### 14.7 界面设计图提示词模板

```text
请生成一张《TeamFlow 管理系统界面设计图》参考图，用于软件工程课程设计报告。
页面类型为：{这里替换成登录页/项目列表页/项目仪表盘页/任务管理页/文档协作页/协作图谱页/风险预警页/AI 助手页}
要求：
1. 必须符合高校课程设计中的小组协作管理系统风格。
2. 页面结构要接近真实 Web 管理后台。
3. 结合 TeamFlow 的功能，例如项目概览、任务看板、进度统计、协作文档、风险预警、贡献分析、AI 规划等。
4. 版式清晰，组件规范，颜色克制。
5. 白底或浅色底，适合插入软件工程课程设计报告，并供人工照着重画。
```

---

## 15. 最推荐的一条“总提示词”

如果后面你想一次性交给别的 AI/Codex，可以直接用下面这段。

```text
你现在要帮助我完成《软件工程》课程设计报告，项目是当前仓库中的 TeamFlow，小组协作过程管理软件方向。请你严格基于当前项目代码、文档和要求.md工作，不允许脱离项目虚构。

你的任务分 5 个阶段完成：

第 1 阶段：先阅读当前仓库中的要求.md、doc/开发文档.md、前后端核心代码、路由、模型和 API，输出项目定位、技术栈、功能模块、已实现内容、扩展设计内容、报告建议目录。

第 2 阶段：列出本报告需要的全部图件，至少包括：环境图、1 层数据流图、系统结构图、用例图、类图、数据库关系图、4 张以上界面设计图。对每张图先输出文字版结构草稿，包括元素、关系、箭头方向、布局建议、图中标签、易错点。

第 3 阶段：逐张生成出图提示词，并使用 gpt-image-2 为每张图生成参考图。所有图必须遵循软件工程绘图规范，白底、整洁、对齐、箭头清晰、文字简洁，适合作为人工重绘底稿。不得使用插画风、海报风、拟物风。

第 4 阶段：根据已经确认的项目事实和图件内容撰写报告正文。正文必须包含可行性分析、需求描述、传统方法学分析、面向对象分析与设计、数据库设计、界面设计、系统实现概述、结论与展望、创新点。传统方法学和面向对象方法学必须明确分开。已实现功能和扩展设计必须区分表述。

第 5 阶段：对最终报告进行审查，检查是否完整覆盖要求.md中的硬性要求，是否存在图缺失、术语不统一、与代码不符、把未实现功能写成已实现等问题，并输出修订后的最终版本。

补充项目事实：
1. 前端技术栈：Vue 3、TypeScript、Vite、Vue Router、Pinia、Element Plus、ECharts、G6。
2. 后端技术栈：FastAPI、SQLAlchemy、Pydantic、JWT、WebSocket。
3. 默认数据库：SQLite。
4. 已有页面：登录、注册、项目列表、仪表盘、任务、消息、文档、文档编辑、文件、协作图谱、贡献分析、风险预警、Git、AI、项目设置。
5. 核心实体：User、Project、ProjectMember、Task、TaskDependency、Milestone、Document、DocumentVersion、Message、CollaborationEvent、ContributionScore、ContributionEvidence、RiskAlert、FileResource、GitRepository、GitCommit、AITaskSuggestion、AIReportHistory。

要求：
1. 每一步先输出结构化内容，再继续下一步。
2. 出图前必须先给文字结构草稿。
3. 图的重点是规范和可重绘性，不是艺术感。
4. 报告语言要符合大学软件工程课程设计写作风格。
```

---

## 16. 最终验收清单

在把结果交给我之前，必须检查：

1. 是否完整覆盖 `要求.md` 中的硬性项目。
2. 是否至少有环境图、1 层数据流图、系统结构图、用例图、类图、数据库图。
3. 是否至少有 4 张以上界面设计图。
4. 所有图是否先有文字版结构说明。
5. 所有图是否符合软件工程绘图规范。
6. 报告是否明确区分“已实现”和“设计扩展”。
7. 报告中的系统功能、页面、实体、数据库表是否能在现有项目中找到依据。
8. 图名、章节名、系统名称是否统一。
9. 是否突出本项目的创新点：协作过程审计、贡献证据链、风险预警、Git 图谱、AI 任务规划与 AI 报告生成。
10. 是否适合我后续手动照着重绘。

---

## 17. 额外提醒

1. GPT Image 2 生成 UML/DFD 类图时，偶尔会出现文字不规整、箭头不标准的问题，所以一定要先出“文字版结构草稿”，再出图，再校对。
2. 如果图片中文字太乱，不要强求一次成功，可以让 AI 减少图中字段和标签，先保住结构正确。
3. 类图和数据库图不要贪多，宁可画核心对象，也不要把整张图挤爆。
4. 界面设计图可以适当美观，但建模图务必以规范清晰为第一优先级。
5. 这份文档的目标不是让 AI 一次性把一切写死，而是让 AI 按正确顺序稳定产出高质量课设报告素材。
